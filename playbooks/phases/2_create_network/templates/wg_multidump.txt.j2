# Automatically generated multidump config for WireGuard debugging
# Format: host:interface:filter[:port][:length]

{% for h in host_data %}
{{ h.hostname }}:{{ wireguard.interface }}:icmp
{{ h.hostname }}:{{ h.underlay_interface }}:udp:{{ h.WG_LISTENING_PORT }}
{% endfor %}
