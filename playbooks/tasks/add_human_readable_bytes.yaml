---
- name: Format {{ label }} as human-readable bytes
  ansible.builtin.set_fact:
    human_readable_bytes: >-
      {{ human_readable_bytes | default({}) |
         combine({(label ~ '_human'): (bytes | int | filesizeformat(true))}) }}
