ansible-galaxy collection install community.network
Turning this set of instructions into an ansible playbook: https://blog.oddbit.com/post/2019-12-19-ovn-and-dhcp/

To use this to create the OVN bridge and switches:
sudo ansible_playbook -i inventory.yaml ./create_ovn.yaml

To cleanup aferwards, merely append --tags cleanup to the above command.
