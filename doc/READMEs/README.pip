### pipreqs
pipreqs . # creates requirements.txt
pipreqs . --force # overwrites existing requirements.txt
pipreqs . --print # prints to STDOUT instead of requirements.txt

### pip
pip install -r requirements.txt
